interface:
  display_name: "Exclusive Server Redev"
  short_description: "Merge prior exclusive branch onto a fresh official tip"
  default_prompt: "Use $woc-exclusive-redev to branch from the official tip and merge the previous exclusive branch (do not reverse merge; do not file-by-file re-port)."
policy:
  allow_implicit_invocation: true
